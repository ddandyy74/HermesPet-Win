<Window x:Class="HermesPet.Views.PinCardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:HermesPet.Views"
        Title="PinCard"
        Width="280"
        Height="124"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    
    <!-- 置顶卡片窗口 -->
    <!-- 参考 macOS: PinCardOverlay.swift PinCardView -->
    
    <Window.Resources>
        <!-- 毛玻璃效果 -->
        <SolidColorBrush x:Key="CardBackground" Color="#E0FFFFFF"/>
    </Window.Resources>

    <!-- 卡片主体 -->
    <Border Background="{StaticResource CardBackground}"
            BorderBrush="#33000000"
            BorderThickness="1"
            CornerRadius="8"
            Padding="14,12,14,10">
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.15" BlurRadius="10" ShadowDepth="2"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 顶部色条（Mode 主色） -->
            <Border Grid.Row="0"
                    Height="3"
                    CornerRadius="1.5,1.5,0,0"
                    Background="{Binding ModeColor}"
                    VerticalAlignment="Top"
                    Margin="-14,-12,-14,0"/>

            <!-- 标题和预览 -->
            <StackPanel Grid.Row="1" VerticalAlignment="Top" Margin="0,6,0,0">
                <!-- 标题 -->
                <TextBlock Text="{Binding Title}"
                          FontSize="13"
                          FontWeight="SemiBold"
                          TextTrimming="CharacterEllipsis"
                          Margin="0,0,0,4"/>

                <!-- 预览（2行） -->
                <TextBlock Text="{Binding Preview}"
                          FontSize="11"
                          Foreground="#666666"
                          TextWrapping="Wrap"
                          MaxHeight="32"
                          TextTrimming="CharacterEllipsis"/>
            </StackPanel>

            <!-- 底部工具栏 -->
            <Grid Grid.Row="2" Margin="0,4,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Mode Label -->
                <TextBlock Grid.Column="0"
                          Text="{Binding ModeLabel}"
                          FontSize="10"
                          Foreground="#888888"
                          VerticalAlignment="Center"/>

                <!-- 时间 -->
                <TextBlock Grid.Column="1"
                          Text="{Binding RelativeTime}"
                          FontSize="10"
                          Foreground="#888888"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"/>

                <!-- 操作按钮 -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                    <!-- 复制按钮 -->
                    <Button Content="📄"
                           Width="24"
                           Height="20"
                           FontSize="11"
                           Background="Transparent"
                           BorderThickness="0"
                           Cursor="Hand"
                           ToolTip="复制到剪贴板"
                           Command="{Binding CopyCommand}"
                           Margin="0,0,4,0"/>

                    <!-- 关闭按钮 -->
                    <Button Content="✕"
                           Width="24"
                           Height="20"
                           FontSize="11"
                           Background="Transparent"
                           BorderThickness="0"
                           Cursor="Hand"
                           ToolTip="关闭"
                           Command="{Binding CloseCommand}"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
